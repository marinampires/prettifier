from setuptools import setup

setup(
    name = "Prettifier",
    version = "0.1",
    packages = ['prettifier', 'tests'],

    # metadata 
    author = "Marina Melo Pires",
    author_email = "marinampires@gmail.com",
    description = "Library to prettifier big number as millions, billions and trillions",
)